{
    "name": "@acme/actions",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "clean": "rm -rf .turbo node_modules",
        "format": "prettier --check . --ignore-path ../../.prettierignore",
        "lint": "eslint ."
    }
}
